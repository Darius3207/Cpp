#include <iostream>
#include <string>
using namespace std;


int main()
{
  
  int x;
  int a;
  int c;
  double w;
  double b;
  
  cout << "Enter x value: ";
  cin >> x;
  cout << "Enter A value: ";
  cin >> a;
  cout << "Enter constant value: ";
  cin >> c;
  
  if (a == c) {
      w = x * pow((a-c), 3);
      b = x + (a, 3);
      
      cout << "The result is: " << w << endl;
      cout << "The result is: " << b;
      }
      
  else {
      w = (x, 3) - a;
      cout << "The result is: " << w;
      }
 
}
